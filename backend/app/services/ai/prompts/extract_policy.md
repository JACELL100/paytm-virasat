# extract_policy (v1, task=extract/vision)

Extract `PolicyExtraction` fields from a policy document's text/image content.
See `app/schemas/documents.py::PolicyExtraction` for the schema and
`app/services/ai/extractor.py::extract_policy` for the live prompt text.
